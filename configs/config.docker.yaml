# Docker Compose 环境配置
# 复制此文件为 config.yaml 后使用

server:
  port: 23523
  reset_gateway_concurrency_on_start: true # Set false when multiple Prism instances share one database
  jwt_secret: "change-this-to-a-secure-random-string"

database:
  host: mysql          # Docker 服务名
  port: 3306
  user: prism
  password: prism123456
  dbname: prism

redis:
  addr: redis:6379     # Docker 服务名
  password: ""
  db: 0

file_storage:
  max_total_size_mb: 1024   # Per-token Files API quota

storage:
  type: ""             # 留空禁用, 或 "tencent" 启用腾讯云 COS
  tencent:
    secret_id: ""
    secret_key: ""
    region: ""
    bucket: ""
    cdn: ""

worker:
  concurrency: 10
  poll_interval: 5s
  max_retry: 3

observability:
  retain_api_call_payloads: false
  api_call_payload_retention_hours: 168
  api_call_payload_max_bytes: 262144
  api_call_payload_encryption_key: ""
  api_call_metadata_retention_days: 90
  api_access_log_retention_days: 30
  audit_event_retention_days: 180
  billing_ledger_retention_days: 365
